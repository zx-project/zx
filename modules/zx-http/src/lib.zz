export using request::{
  RequestOptions,
  request,
  head,
  get,
}

export using response::{
  Response,
}

export using method::{
  DELETE,
  GET,
  HEAD,
  PATCH,
  POST,
  PUT,
  OPTIONS
}

export using init::{
  init
}
