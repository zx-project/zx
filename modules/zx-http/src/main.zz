using <stdio.h>::{ printf }
using zx_http as http

fn main (int argc, char **argv) -> int {
  int mut exit_code = 0;
  if argc > 1 {
    unsafe {
      printf("%s\n", argv[1]);
    }
  }
  return exit_code;
}
