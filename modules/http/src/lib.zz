export using method::{
  DELETE,
  GET,
  HEAD,
  PATCH,
  POST,
  PUT,
  OPTIONS
}

export using header::{
  Header
}

export using init::{
  init
}

export using response::{
  Response,
}

export using request::{
  RequestOptions,
  request,
  head,
  get,
}
